# cursor-cloud

Cloud Agents ops workspace for Remnawave / VPN operations.

This repository is the linked workspace for Cursor Cloud Agents. Use it for ops notes, scripts, and non-secret configuration only.

**Do not commit secrets** (API tokens, passwords, private keys, `.env` values, or credentials). Keep those out of public files and chat-committed artifacts.
